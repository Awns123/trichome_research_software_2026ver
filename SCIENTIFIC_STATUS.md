# 과학적 결과의 현재 상태

## 판정

2026년 원 논문이 보고한 “여섯 가지과 종의 모용 형태거리와 분자계통거리가 양의 대응을 보인다”는 결론은 **현재 유지하지 않습니다**. 보관된 *Brugmansia suaveolens* `matK` 서열의 방향 문제를 교정했을 때 양의 상관이 재현되지 않았기 때문입니다.

이 판정은 “모용 형태에 계통 신호가 없다”는 반대 결론을 증명하지도 않습니다. 여섯 종과 두 plastid marker는 진화적 일반화를 하기에는 정보량과 검정력이 작습니다.

## 결과 층위

| 층위 | Spearman | Pearson | 의미 |
|---|---:|---:|---|
| 보관된 원 계통수 | `r_M=0.775`, 단측 exact `p=0.044444` | `r_M=0.865307`, 단측 exact `p=0.045833` | 원 논문의 역사적 보고 결과 |
| *B. suaveolens* 제외 | `r_M=0.333`, 논문 보고 양측 `p=0.575` | — | 원 신호가 종 구성에 민감함을 보여 줌 |
| 원 FASTA를 저장 상태로 새 pipeline에서 재분석 | `r_M=0.771429`, 단측 `p=0.041667` | `r_M=0.861809`, 단측 `p=0.041667` | 새 pipeline이 원 신호를 가깝게 회수함 |
| 기존 accession을 NCBI feature 방향으로 재추출 | `r_M=-0.242857`, 단측 `p=0.676389`, 양측 `p=0.568056` | `r_M=-0.127559`, 단측 `p=0.559722` | 방향 교정 뒤 원 양의 신호가 사라짐 |
| 표준화 sequence-set 민감도 분석 | `r_M=-0.103571`, 단측 `p=0.541667` | `r_M=-0.080054`, 단측 `p=0.480556` | 다른 accession 정책에서도 유의하지 않음 |

모든 exact p-value는 여섯 taxon label을 분자거리 행렬에 동시에 재배치한 `6!=720`개 순열을 전수 계산한 값입니다. 15개의 행렬 상삼각 원소를 서로 독립인 표본으로 간주해 섞은 검정이 아닙니다.

## 확인된 주요 입력 문제

1. *B. suaveolens* `matK` (`ON528858.1`)가 주석된 유전자 방향의 역상보 서열이 필요한 상태로 보관돼 있었습니다.
2. 전체 17종 자료의 *Pilea glauca* `rbcL` accession은 실제로 *Picea glauca*였습니다.
3. *Antennaria dioica* `rbcL`에도 방향 문제가 있었습니다.
4. *Centaurea cyanus* `matK`는 공통 barcode window와 잘 겹치지 않는 partial 구간이었습니다.
5. 기존 tree에는 *Urtica dioica*가 있었지만 ZIP에 대응 raw FASTA와 accession 기록이 없었습니다.

여섯 가지과 종의 결론 변화와 가장 직접적으로 일치하는 진단은 *Brugmansia* `matK` 방향 교정 후 비정상적으로 긴 *Brugmansia* 거리가 축소된 것입니다. 그러나 전체 tree에는 여러 문제가 함께 있었으므로 “오직 한 오류만이 유일한 원인”이라고 단정하지 않습니다.

## 감사의 귀속

2026년 7월 감사는 Codex 보조 독립 재분석이며, 이 저장소에 보존된 상태에서는 학생의 독립적인 전체 재실행이 완료되지 않았습니다. 감사 결과를 원 연구 당시 학생 수행으로 소급하지 않습니다. `post_submission_audit/`의 검산기는 저장된 행렬에서 수치를 확인하기 위한 2026-08-26 공개 준비 도구이며 역사적 학생 코드가 아닙니다.

